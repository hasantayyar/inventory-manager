{% extends '::base_admin.html.twig' %}

{% block content -%} 
    <h1>Car creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_car') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
